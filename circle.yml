## Customize dependencies
dependencies:
  pre:
    - npm install -g bower
    - bower install

  cache_directories:
    - "bower_components"

## Customize test commands
test:
  override:
    - npm run test-single-run
